{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "balance": {
          ".write": false
        },
        "transactions": {
          ".write": false
        }
      }
    }
  }
}
