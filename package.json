{
  "name": "reza-ewallet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp *.html public/ && cp manifest.json public/ && cp service-worker.js public/"
  },
  "dependencies": {
    "@vercel/analytics": "^1.6.1",
    "@vercel/speed-insights": "^1.3.1"
  }
}
