{
  "name": "reza-ewallet",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp *.html public/ && cp manifest.json public/ && cp service-worker.js public/"
  }
}
